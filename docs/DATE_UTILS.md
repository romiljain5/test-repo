# Date Utilities

Helper functions for common date operations.

## Functions

- `formatDate(date, format)` - Format a date to string
- `daysBetween(date1, date2)` - Calculate days between two dates
- `isWeekend(date)` - Check if a date is Saturday or Sunday
- `addDays(date, days)` - Add days to a date
